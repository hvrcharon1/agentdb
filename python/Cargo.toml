[package]
name    = "agentdb-python"
version = "0.2.0"
edition = "2021"

[lib]
name      = "agentdb"
crate-type = ["cdylib"]

[dependencies]
agentdb = { path = "..", features = ["python"] }
pyo3    = { version = "0.21", features = ["extension-module"] }
serde_json = "1"
