# Codecov configuration for AgentDB
# https://docs.codecov.com/docs/codecov-yaml

coverage:
  precision: 2
  round: down
  range: "60...100"

  status:
    project:
      default:
        # Fail CI if overall coverage drops below 70%.
        # Ratchet to 80% before v1.0.0.
        target: 70%
        threshold: 2%
        if_not_found: success
        if_ci_failed: error
    patch:
      default:
        # New code added in a PR must be at least 70% covered.
        target: 70%
        threshold: 2%
        if_not_found: success
        if_ci_failed: error

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no

comment:
  layout: "header, diff, flags, components, footer"
  behavior: default
  require_changes: false

ignore:
  - "python/**"
  - "nodejs/**"
  - "examples/**"
  - "benches/**"
