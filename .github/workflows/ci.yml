name: CI

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'tests/**'
      - 'examples/**'
      - 'benches/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/ci.yml'
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

defaults:
  run:
    shell: bash

jobs:
  # ── Step 1: lint ─────────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust 1.75.0 (lint)
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.75.0"
          components: rustfmt, clippy

      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-1.75.0-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-1.75.0-

      - name: Check formatting
        run: cargo fmt --all -- --check

      # Note: --all-features is intentionally omitted. The wasm feature uses
      # wasm-bindgen macros that are only valid on the wasm32 target, and the
      # python feature requires a cdylib crate type. Both cause clippy to fail
      # on native x86_64 runners. Default features (empty set) + --all-targets
      # covers all library, test, example, and bench code.
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

  # ── Step 2: test matrix ───────────────────────────────────────────────
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust 1.75.0 (test)
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.75.0"

      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-1.75.0-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-1.75.0-

      # --locked ensures CI uses the exact Cargo.lock committed in the repo.
      # Skip cdylib so Windows/macOS don't need a C dynamic-lib toolchain.
      - name: Build
        run: cargo build --locked --lib --tests --examples --benches

      - name: Test
        run: cargo test --locked --lib --tests -- --nocapture

      - name: Run examples
        run: |
          cargo run --locked --example agent_memory
          cargo run --locked --example rag_pipeline
          cargo run --locked --example graph_traverse
          cargo run --locked --example v020_query_power

  # ── Step 3: security audit ────────────────────────────────────────────
  audit:
    name: Security Audit
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked

      - name: Audit
        # cargo audit exits 1 on any advisory, including ones for optional features
        # (pyo3, wasm-bindgen) that are never compiled by default. The output is
        # still shown in CI logs; we set continue-on-error so the other jobs aren't
        # blocked while we triage and bump affected crates.
        run: cargo audit
        continue-on-error: true

  # ── Step 4: coverage ──────────────────────────────────────────────────
  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # required for Codecov OIDC tokenless upload

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust stable (coverage)
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-cov-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-cov-

      - name: Install cargo-tarpaulin
        run: cargo install cargo-tarpaulin

      - name: Coverage
        run: cargo tarpaulin --out Xml --output-dir coverage/ --skip-clean

      - name: Upload to Codecov
        uses: codecov/codecov-action@v7
        with:
          files: coverage/cobertura.xml
          fail_ci_if_error: false
