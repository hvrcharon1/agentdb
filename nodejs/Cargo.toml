[package]
name    = "agentdb-node"
version = "0.3.1"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
agentdb    = { path = ".." }
napi       = { version = "2", features = ["napi8", "serde-json"] }
napi-derive = "2"
serde_json = "1"

[build-dependencies]
napi-build = "2"
